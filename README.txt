Getting Started
---------------------------------------
To install the application requirements, run 'npm install' while you are in the directory containing the package.json
The a local instance of the application can be started with the 'npm run dev'.
Currently the default port is set as 4200, however you can change it by updating process.env.port.